# Package

version       = "0.1.0"
author        = "honewatson"
description   = "String validation for nim.  Validate your tainted strings via a table."
license       = "MIT"
srcDir        = "src"

# Dependencies

requires "nim >= 0.18.0"
requires "regex >= 0.1.0"
